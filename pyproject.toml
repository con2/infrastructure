[project]
name = "infrastructure"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible"
]

[tool.uv]
constraint-dependencies = [
    "ansible-core<2.20"
]
