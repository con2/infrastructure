server {
    listen 80;
    server_name {{ atlasso_allowed_hosts }};

{% if atlasso_letsencrypt %}
    location /.well-known/acme-challenge/ {
        alias /srv/letsencrypt/challenges/;
        try_files $uri =404;
    }

{% endif %}
{% if atlasso_ssl %}
    location / {
        return 302 https://$host$request_uri;
    }
}

server {
    listen 443 http2;
    server_name {{ atlasso_allowed_hosts }};

    add_header Strict-Transport-Security max-age=15768000;

    ssl on;
    ssl_certificate {{ atlasso_ssl_certificate }};
    ssl_certificate_key {{ atlasso_ssl_certificate_key }};

{% endif %}
    location ~ ^/static/(.*)$ {
        alias /srv/{{ atlasso_hostname }}/public_html/static/$1;
        expires 30m;
    }

    rewrite ^/favicon.ico$ /static/favicon.ico last;
    gzip_static on;

    location / {
        proxy_pass http://localhost:{{ atlasso_port }};
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
